# 📜 Манифест разработки микросервисов в стиле Performance‑Driven Development

> Мы проектируем от метрик и реальных нагрузок.  
> Производительность — это требование, а не побочный эффект.  
> Каждое решение измеримо, оборотно и прозрачно.

---

## 1. Домен важнее фреймворка
- Формулируй бизнес‑правила и сущности в чистом виде (**domain layer**).
- Определи **инварианты** (что всегда должно быть верно) и **use‑cases**.
- Не привязывайся к конкретной БД, брокеру или веб‑фреймворку на старте.

## 2. Изоляция слоёв
- **API слой** — приём/отдача данных, валидация, маппинг DTO.
- **Application слой** — оркестрация домена и инфраструктуры.
- **Domain слой** — чистая логика, без зависимостей.
- **Infrastructure слой** — адаптеры к внешнему миру.
- Зависимости направлены внутрь (**Dependency Inversion**).

## 3. Внедрение зависимостей через интерфейсы
- Определи протоколы/абстракции для репозиториев, клиентов, шины событий.
- Реализации подсовывай через DI (`Depends` в FastAPI или контейнеры в других фреймворках).
- Лёгкая подмена адаптеров и тестирование без реальных сервисов.

## 4. Тестирование параллельно с кодом
- **Unit‑тесты** для домена и use‑cases.
- **Интеграционные** для адаптеров (БД, брокеры).
- **Контрактные** для API и событий.
- Запуск тестов в CI для предотвращения регрессий.

## 5. Читаемость кода
- Маленькие файлы и классы, одна ответственность на модуль.
- Говорящие имена, минимум «магии».
- Докстринги для публичных методов и сложных мест.
- Консистентный стиль (`ruff` / `black` / `isort`).

## 6. Наблюдаемость с первого дня
- Логи с контекстом (`trace_id`, `user_id`).
- Метрики (Prometheus): RPS, latency, ошибки.
- Трейсинг (OpenTelemetry) для распределённых вызовов.
- Health/ready‑эндпоинты для оркестратора.

## 7. Планирование масштабирования
- Stateless‑архитектура для горизонтального масштабирования.
- Очереди/шины для фоновых задач и интеграций.
- Кэш для тяжёлых read‑операций.
- Идемпотентность для повторных запросов.

## 8. Управление конфигурацией
- Всё через ENV (`pydantic‑settings`).
- Разделение конфигов по окружениям (dev/stage/prod).
- Не хранить секреты в коде.

## 9. Автоматизация
- `Makefile` или `invoke` для типовых команд.
- CI/CD пайплайн: **lint → test → build → deploy**.
- Автоматические миграции БД при деплое.

## 10. Рефакторинг как процесс
- Улучшай код при каждом изменении, не откладывай.
- Убирай дубли, выноси общее в модули.
- Следи за зависимостями — не тащи лишние пакеты.

---

### 💡 PDD‑мантра
> **Сначала метрика — потом код.**  
> **Сначала домен — потом фреймворк.**  
> **Сначала простота — потом оптимизация.**
